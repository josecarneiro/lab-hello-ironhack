import React, { Component } from 'react';

import './Button.css'

class Button extends Component {
    render () {
        return (
            <div className="buttons">
                <button className="landing-button">Awesome!</button>
            </div>
        );
    }
}

export default Button;
